# Manuscript/certificate reconciliation — 2026-08-08

## Claim boundary

- **PROVED:** The manuscript's final upper bound is the canonical-coordinate
  bound `d_w = 2^(w^2-1)` at every pair and internal cut.
- **PROVED:** `R_infinity(w)` is a maximum over canonical cuts. Generic
  tightness means that at least one such cut reaches `d_w`, not that every cut
  does.
- **PROVED:** For `w=3`, the certified canonical binary profile reaches 256.
  The earlier pair/internal bounds 512/1024 were pre-quotient upper bounds.
  The raw internal rank 512 was observed before full symplectic transport and
  becomes the canonical adjacent profile 256/256/256.
- **PROVED:** Cycle 8 v2 states generic nonuniform saturation
  `R_infinity(w)=2^(w^2-1)` for every `w>=3`, with `n=11` uniformly
  sufficient. Width three is witnessed first on `G_(10,3)` and embeds into
  `G_(11,3)` under the nested canonical construction.
- **NOT PROVED:** homogeneous-anisotropic or isotropic generic tightness,
  nonvanishing at any critical point, compression below `d_w`, or a cubic
  thermodynamic limit.

## Frozen-record map

| Question | Authoritative frozen record | Manuscript use |
|---|---|---|
| `w=3` longitudinal saturation | `cycle-6-b6-lane-b-longitudinal-saturation-v1.json` | canonical rank reaches 256 |
| arbitrary-width canonical closure | `cycle-7-b7-lane-b-arbitrary-width-closure-v1.json` | all canonical pair/internal cuts have upper bound `d_w`; raw 512 is a coordinate artifact |
| arbitrary-width generic tightness | `cycle-8-b11-g1-generic-tightness-v2.json` | `R_infinity(w)=d_w`, generic nonuniform weights, uniform sufficient length 11 |
| abstract H3 obstruction | `cycle-10-b9-abstract-separator-k33-sharpness-v1.json` | pair bound need not imply the internal bound |

The sealed artifacts and their frozen proof files were not modified in this
audit.

### Replay-infrastructure finding

- **PROVED:** The Cycle 6 builder reaches and passes its mathematical
  regression assertions and frozen-input hash checks, but its final bytewise
  `--check` fails. The frozen payload includes per-case
  `transfer_wall_seconds = round(perf_counter difference, 6)`, so rebuilding
  cannot deterministically reproduce the JSON bytes. This is a sealing-
  scaffold defect, not evidence of a changed rank: the builder would have
  stopped earlier on any upper-bound or saturation regression.
- **OPEN AUDIT:** Because the failed builder does not retain the newly rendered
  payload, this pass has not yet proved that timing fields are the *only*
  bytewise differences. The Cycle 6 mathematical replay and its immutable-byte
  replay must therefore be reported separately.
- **PROVED:** Cycle 7, Cycle 8 v2, and Cycle 10 passed their immutable
  byte-for-byte `--check` replays in this audit. In particular, Cycle 7
  certifies the canonical `d_w` upper bound, Cycle 8 v2 certifies generic
  nonuniform saturation and uniform sufficient length 11, and Cycle 10
  certifies the `K_(3,3)` internal-cut obstruction.

## Manuscript repairs

1. **PROVED:** H1 now defines partial chains by
   `supp(boundary c) subset S`, so the discrete Stokes step has the stated
   domain.
2. **PROVED:** H3 now defines `rho_i(m)` by equal evaluation on all exposed
   partial chains with trace `m`; the separator proof explicitly invokes this
   well-definedness. The erroneous reference to a pre-Arf
   `lambda_a_i lambda_b_i` term was removed; that product belongs to the final
   Arf weight.
3. **PROVED:** The canonical basis proof now uses an inductive symplectic
   complement construction that retains old handle pairs and appends the new
   slab block. It no longer infers nesting from a lexicographically least
   solution.
4. **PROVED:** The genus proof now displays the `w=3` counts
   `(V,E,F)=(9n,21n-9,10n-5)` and the even-grid lower bound
   `g >= 1+(E-2V)/4`.
5. **PROVED:** `R_infinity(w)` is defined before first use, the title says
   “cubic-lattice,” and the old `cref -> ref` redefinition was replaced by
   the actual `cleveref` package when installed, with an explicit `autoref`
   fallback for the repository's minimal TeX environment.

## Primary-source citation audit

- **PROVED:** Gobron's source numbering makes the multicomplex-Pfaffian result
  Theorem 3.2 and the nonorientable-genus complex-Pfaffian expansion
  Corollary 3.4. The manuscript citation is correctly scoped.
- **PROVED:** Bravyi's paper explicitly describes genus-dependent contraction
  of matchgate tensor networks by Grassmann Gaussian methods. The manuscript
  makes no unsupported theorem-number attribution.
- Cimasoni Theorem 2.1 and Millichap--Salinas Theorem 4 retain the previously
  completed source audit; this pass did not alter those attributions.

## Falsifiers retained

- A canonical `w=3` certificate with rank 512 or 1024 would contradict the
  paper. No such certificate exists in the frozen record map above.
- Failure of old-pair retention under terminal-slice extension would invalidate
  the uniform-`n=11` width-three embedding argument. The manuscript now states
  the symplectic-complement mechanism that must be checked by replay.
- A `K_(3,3)` internal flattening of generic rank two would make H3 appear
  redundant. The frozen rank-four minor prevents that inference.
