# Lane B character-duality correction release

- Author: Hainan Zhao
- Corrected version DOI: `10.5281/zenodo.21847231`
- Concept DOI: `10.5281/zenodo.21845272`
- Superseded version DOI: `10.5281/zenodo.21845273`

The first public version used the longitude cocycle `PD(b_i)`, which extracts
`x_i`, in the internal-cut H3 step where the `lambda_(b_i)` character requires
the meridian cocycle `PD(a_i)`, which extracts `y_i`.  The corrected version
withdraws that step and replaces it with the arbitrary-width pushed-off
meridian proof.  It also records the correct triangular transport:

    alpha_j=tilde alpha_j+sum_k U_(kj) beta_k,
    beta_j=tilde beta_j.

Cycle 18 freezes the false step, the repaired proof, the exact `GF(2)`
coordinate firewall, two-prime canonical binary-rank replay, and the
independent denominator-free core replay.  No thermodynamic or exact-solution
claim is added.
