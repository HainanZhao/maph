# Cycle 18 failure ledger

1. **FALSE STEP — longitude used for H3.**  The released manuscript used the
   exposed longitude cocycle `PD(b_i)` to justify the un-emitted
   `lambda_(b_i)` character.  But `PD(b_i)` extracts `x_i`, while
   `lambda_(b_i)` multiplies `y_i`.  The corrected proof uses the pushed-off
   meridian cocycle `PD(a_i)`.

2. **FALSE STEP — triangular correction assigned to rho.**  The released
   manuscript added the triangular longitude correction to the H3 function
   `rho_i`.  In fact `b_j=tilde b_j+sum_k U_(kj)a_k` changes
   `alpha_j=PD(b_j)` and leaves `beta_j=PD(a_j)` exactly fixed.  The correction
   belongs to the `lambda_a` character factor, not H3.
