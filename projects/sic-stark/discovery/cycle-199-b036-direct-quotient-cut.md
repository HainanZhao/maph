# Rejected direct-quotient cut

`OBSERVED`: the full 24-mode carrier has 145 exact aliases per finite row in
the preregistered window; all 10,440 variable divisor audits were finite, and
the normalized three-step source multiplier is nonconstant.  This does **not**
obstruct a helical/Zak construction: a genuine Poincare periodization sums the
alias fibre rather than requiring its summands to be equal.  The termwise
quotient model is therefore rejected and supplies no seal or gate change.

The live question remains quotient-level meromorphic periodization and its
endpoint continuation.

`OBSERVED` (test-only): the first Abel-pole regression run failed because its
assertion expected the prose words “below/above” while the exact record uses
the equivalent signs “negative/positive.”  The formula and data agreed; the
assertion was corrected before any promotion.
