# C202 working note

`OBSERVED`: the first regression invocation failed before any claim check
because the C202 verifier imported its C198 sibling as a script-local module.
The import is corrected to the repository `proof` package. This is a replay
packaging repair, not mathematical evidence and not a change to the frozen
question.
