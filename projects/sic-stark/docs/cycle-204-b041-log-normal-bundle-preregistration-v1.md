# Cycle 204 / B041 preregistration: logarithmic normal bundle

Cycle 203 rejected only an ordinary inverse normal vector. This block tests the
distinct logarithmic/b-normal category: `s*d/ds` and `ds/s` are regular,
coordinate-invariant b-objects. It must determine whether that category
actually changes the Abel-rate representation enough to produce a fixed
weight-zero all-row map, or merely provides an invariant line-valued record.

<!-- research-freeze-v1
{
  "schema": "research-preregistration-freeze-v1",
  "cycle": 204,
  "parameters": {
    "b_geometry": {
      "kind": "expression",
      "value": "On the frozen A_6 axis gamma(s), define the b-tangent generator V_b=s*d/ds and b-cotangent generator eta_b=ds/s on the punctured compactification. Under every s_c=c*s with c>0 and under A_6:s->beta^(-6)*s, both V_b and eta_b are invariant. Keep them distinct from ordinary ds and [ds]^(-1).",
      "rationale": "This is the new category identified by the C203 companion review, with its transformation law frozen before use."
    },
    "two_scale_action": {
      "kind": "expression",
      "value": "Use G_lambda=(B,lambda*R) and the 36-dimensional regular packet space E_reg from Cycles 200--202. Regulator dilation lambda->q*lambda, q in {2,3,5}, fixes V_b and eta_b because they depend only on the geodesic coordinate. Audit the tensor objects R, R tensor eta_b, and contraction/integration candidates with V_b or eta_b; no source identity may be assumed between lambda and s beyond u_lambda(s)=exp(-lambda*s).",
      "rationale": "It directly tests whether b-geometry cancels the actual rate weight rather than a coordinate weight."
    },
    "admissible_map_class": {
      "kind": "expression",
      "value": "A pass requires an equation-(66)-derived b-geometric pairing or source theorem that maps one declared tensor object linearly to the fixed C198 T_6 values, is independent of lambda, and succeeds on all 36 rows. A canonical line-valued object with residual rate weight is recorded separately and is not a scalar endpoint map.",
      "rationale": "The test allows the b-category to help but prohibits declaring its formal symbols to be a missing pairing."
    },
    "claim_boundary": {
      "kind": "expression",
      "value": "A no-go rejects only b-objects generated by V_b and eta_b without an additional source density theorem: it leaves a new equation-(66) b-pairing theorem, covariant target line, nonlinear/higher-germ/non-Abel continuation, AFK, fusion, Stark, and TCC open. A positive result proves only the stated b-geometric bridge.",
      "rationale": "The category shift is a genuinely new engine, not a broad impossibility assertion."
    }
  },
  "resource_caps": {
    "character_rows": {"kind":"integer","value":36,"rationale":"Complete normal packet and C198 target grid."},
    "b_generators": {"kind":"integer","value":2,"rationale":"V_b and eta_b only."},
    "regulator_dilation_probes": {"kind":"integer","value":3,"rationale":"q in {2,3,5}."},
    "tensor_candidates": {"kind":"integer","value":3,"rationale":"R, R tensor eta_b, and the frozen contraction/integration candidate class."},
    "wall_seconds": {"kind":"integer","value":180,"rationale":"Exact all-row transformation ledger."}
  },
  "formula_families": [
    "Cycle-203 A_6 axis and normal-line rescaling audit",
    "Cycle-201 regulator action on the two-scale germ",
    "Cycle-202 source normal datum and fixed C198 target weights"
  ],
  "selection_rule": [
    "Verify every b-generator under both symbolic positive coordinate rescaling and every q in {2,3,5} regulator dilation.",
    "Enumerate all 36 rows for each declared tensor candidate before testing a target map.",
    "Require an explicit equation-(66) source formula for any pairing; otherwise classify the object as line-valued/residual-weight data."
  ],
  "failure_rule": [
    "If b-generators are rate weight zero, tensoring them with R leaves R at weight one and no direct fixed-target linear equality may be claimed.",
    "An unproved b-integral, a selected scale, a division by lambda, or a target-fitted pairing is outside the block and does not pass.",
    "Do not promote b-covariance to a Zak map, AFK, fusion, Stark, or TCC claim."
  ],
  "pre_execution": {
    "timestamp_utc": "2026-08-03T07:25:00Z",
    "git_head": "05a5dd2de455d523343430c7abb91eff34768062",
    "git_state": "DIRTY from the concurrent repository-wide PROGRAM header migration and unrelated projects/tools, plus this live Cycle-204 preregistration. This block freezes only the listed SIC--Stark mathematical inputs."
  },
  "input_paths": [
    "artifacts/cycle-203-b040-inverse-normal-line-v1.json",
    "artifacts/cycle-202-b039-normal-derivative-target-weight-v1.json",
    "artifacts/cycle-201-b038-two-scale-germ-covariance-v1.json",
    "artifacts/cycle-200-b037-regular-residue-jet-v1.json",
    "proof/verify_cycle_203_inverse_normal_line.py",
    "proof/verify_cycle_202_normal_derivative_target_weight.py",
    "proof/verify_cycle_201_two_scale_germ_covariance.py",
    "proof/verify_cycle_200_regular_residue_jet.py",
    "../../tools/preregistration_check.py"
  ]
}
-->
