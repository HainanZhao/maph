# Version 1.5 main-paper integration

Version 1.5 changes packaging and exposition, not the certified
RQ-000013 identity.

- The complete certified RQ-000013 addendum is integrated into the
  quadratic-support section of the main manuscript.
- The standalone addendum PDF and TeX source are not top-level Zenodo
  files in version 1.5.
- The main upload is named
  `effective-stark-results-00-main-paper.pdf`, so it sorts before all
  other release files.
- The deterministic version 1.5 companion nests the immutable
  published version 1.4 companion. The historical standalone addendum,
  its hashes, and all prior correction records therefore remain
  preserved.

Claim boundary:

- `PROVED`: RQ-000013 has exact \(E_\chi=2\), \(I_\chi=2\), and
  \(X_{[0]}=u^2,\ X_{[1]}=u^{-2}\), by the uniform Engine-A theorem
  after exact case-specific checks.
- `OBSERVED`: the quarantined `bnrL1` point comparison is not used in
  the proof.

Reserved version DOI: `10.5281/zenodo.21713178`.
