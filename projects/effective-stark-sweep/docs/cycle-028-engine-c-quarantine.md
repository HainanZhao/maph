# Cycle 028 — Engine-C tool quarantine

Four packets are quarantined: three reproducible PARI 2.15.4
`bnrclassfield`/segmentation failures and one expensive resolvent
interrupted without a mathematical verdict.  The exact inputs and
failure history are retained in
`data/engine-c-tool-quarantine-v1.json`.
