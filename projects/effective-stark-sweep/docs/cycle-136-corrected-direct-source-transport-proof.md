# Cycle 136 — corrected direct-source transport proof

For each of the ten Cycle-135 targets, the corrected integral-basis
screen gives an equality of moduli
\(\mathfrak m_t=\mathfrak m_s\mathfrak q\), with \(\mathfrak q\)
coprime to the source finite modulus. The exact target-to-source
ray-class map preserves identity and the distinguished sign class and
has recorded cyclic generator coefficient \(c\).

For the distinct prime factors \(\mathfrak q_j\) of
\(\mathfrak q\), with source ray logs \(\ell_j\), Euler deletion is

\[
 L_{\mathfrak m_t}(s,\chi)=
 \prod_j(1-\chi(\mathfrak q_j)N\mathfrak q_j^{-s})
 L_{\mathfrak m_s}(s,\chi).
\]

Every character surviving the one-place differenced Fourier transform
is odd at the distinguished real place, hence its source (L)-value
vanishes at zero. Differentiation and Fourier inversion therefore give

\[
 X_{\mathfrak m_t}(a)=
 \prod_{J}X_{\mathfrak m_s}
 \left(ca-\sum_{j\in J}\ell_j\right)^{(-1)^{|J|}.
\]

The sealed source packets are positive at the frozen split real
embedding. Consequently every displayed product or quotient has the
required positive orientation. The exact source data and transcripts,
the corrected geometry audit, and each factor log are hash-pinned in
`artifacts/corrected-direct-source-transports-v1.json`.

This proves the ten named Artin-labelled transports. It does not claim
that any of the remaining Engine-B members has a packet identity.
