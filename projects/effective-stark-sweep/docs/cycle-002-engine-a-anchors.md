# Cycle 002 — Engine A reproduction

Date: 2026-07-29

The two frozen Engine-A bundles reproduced from the internal
`sic-stark` source tree:

- `A-d4-ray4`: `bnfcertify`, ray structure, and the oriented target
  unit marker passed;
- `A-d8-ray8`: both regulator indices equal two, both `bnrL1` units
  were emitted, and the unconditional quadratic packet marker passed.

The raw command output is preserved in
`artifacts/anchor-transcripts/`; its hashes are in
`artifacts/anchor-reproduction-v1.json`.  Status: `VERIFIED`.
