# Cycle 125 — B5-025 label-aware transport proof

Let (c=5) be the exact target-to-source generator coefficient in
(mathbb Z/6mathbb Z).  The identity and sign labels are preserved.
For a target label (a), the source label is consequently (ca).
If the distinct primes in the coprime modulus quotient have source
ray logs (l_i), the imprimitive Euler product and rank-one vanishing
give

\[
 X_{\mathfrak m'}(a)=
 \prod_{J}X_{\mathfrak m}\left(ca-\sum_{j\in J}l_j\right)^{(-1)^{|J|}.
\]

This is the same exact label-aware Euler-deletion argument proved in
Cycle 118, now instantiated with the fresh B5-025 geometry gates.  The
sealed source entries are positive at the frozen split embedding, so
the product/quotient has the required orientation.  Prime exponents
do not repeat an Euler factor.
