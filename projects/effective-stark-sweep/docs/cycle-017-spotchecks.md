# Cycle 017 — ten pre-bulk spot checks

The frozen selection contains two A, two B, two C, and four FRONTIER
rows. The frontier rows cover all three observed obstruction types.
Every case was rebuilt individually from the ideal census and all
10/10 checks passed before any bulk identification queue was opened.

Artifacts: `data/w1-spotchecks-v1.json` and
`artifacts/w1-spotchecks-v1.json`.
