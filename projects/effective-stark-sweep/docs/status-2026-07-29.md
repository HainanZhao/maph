# Effective-Stark Sweep status

Date: 2026-07-29 UTC

## State

`CYCLES_001_020_COMPLETE`

Publication and correspondence metadata have no active research-gate
role. The mathematical source and reproduction gate passed 7/7.

Completed:

- all seven Paper-I/II proof bundles reproduced end-to-end;
- 121 real quadratic fields certified;
- 13,939 ideals enumerated, giving 8,200 exact conjugacy-deduplicated
  maximal-order cases;
- exact sign-class Fourier support implemented;
- structural A/B/C screens implemented and checked against all seven
  historical routes;
- preregistered 66-case W1 pilot completed.

Pilot output: 59 provisional A routes, one provisional B route, five
provisional C routes, and one `INDEX_GT_2` frontier. No new packet is
yet tagged `PROVED`.

The full 8,200-case W1 run is complete: 5,459 A, 655 B, 817 C, and
1,269 FRONTIER. Ten spot checks pass and the structural yield gate
passes with 6,928 candidates beyond known anchors.

For `RQ-000190` over \(\mathbb Q(\sqrt7)\), W2 is `VERIFIED` with two
independent imaginary-base reconstructions and safe exponent 4032.
This proves unconditional order-six powered algebraicity. The exact
candidate packet is now also `VERIFIED`: an exact nine-point
Yamamoto-cone formula at each ray class was enclosed using Arb, with no
PARI \(L\)-value in the proof chain. The powered height upper bound is
\(9.20\times10^{-9}\), the degree-3-to-24 Voutier lower bound is
\(5.22\times10^{-5}\), and the certified margin is \(>5688\), against
the required \(100\).

## First research queue

1. Apply the complete CM/V4 predicate battery to the 817 C-screen
   candidates.
2. Apply W2 to the remaining 654 B candidates.
3. Run exact conductor/regulator identification on the A queue,
   removing trivial/duplicate packets before the Phase-1 yield count.
4. Add the conductor-lowered nonmaximal-order census layer.

## Integrity

- full anchor gate: `artifacts/anchor-reproduction-v1.json`;
- W1 anchor regression: `artifacts/w1-anchor-regression-v1.json`;
- ideal backbone: `artifacts/frozen-ideal-census-v1.json`;
- pilot records: `artifacts/w1-pilot-v1.json`;
- pilot transcript: `artifacts/w1-pilot-v1.transcript`;
- cycle report: `docs/cycles-001-010-summary.md`.
- order-six Arb packet certificate:
  `artifacts/q7-p7-w3-arb-certificate-v1.txt`.

The sibling internal source directory present on this machine is
`projects/sic-stark`; no separate `sic-start` directory was found.
