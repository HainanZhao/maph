# Effective-Stark sweep status — 2026-07-30

## Cycle 071 results-paper freeze

The proxy-clean results manuscript is frozen at
`paper/effective-stark-results.tex` with a compiled eight-page PDF.
The internal referee audit passes every displayed exponent, margin,
polynomial, finite constant, Artin label, certificate hash,
conditionality disclaimer, and Stark-1980 use. The one-cycle parity
bonus is proved and included: nontrivial one-place support forces even
genuine Shintani index.

The audit found one documentation-only sign omission in the v2
general-\(e\) specialization strings. Version 3 restores the minus
signs; the general formulas, sealed case records, theorem tags, and
numerical certificates were already correct.

Paper III's two diagnostics were not pending: Cycles 155--157 already
banked the favorable central-integral rehearsal, adverse growing
periodization, and the Fourier-normalization stop decision. The paper
compiles with those results and remains honestly conditional on the
missing coefficient-to-ray-logarithm map. The DST/Cohen--Roblot
comparison table has started as a background task.

## Census v5 banked

All proxy recovery is complete. The genuine occurrence histogram is
3,899 trivial, 1,560 Engine A, 232 Engine B, 881 Engine C, and 1,628
FRONTIER. Engine B has 88 distinct normal closures; Engine C has 447
packet fields and 1,361 packet occurrences. The exact frontier
taxonomy is 1,088 index, 502 cap, 31 unit, five tool, and two
real-place failures.

The genuine frontier shares are 8.42%, 19.53%, 24.58%, and 28.53% by
norm quartile, so strict monotonicity survives. All 446 genuine odd
indices above two occur on empty-support trivial rows; there is no
substantive odd-index frontier. W4 now waits only on B occurrence
transport.

## Cycle 069 containment update

R-13 is effective: every predicate is GENUINE or PROXY, aggregates may
be MIXED, and no effective `VERIFIED_*` tag rests on proxy data.
RQ-007500 genuinely re-passes: its actual degree-32 splitting field has
group `[32,38]`, equals the historical polynomial, and is reconstructed
independently from both \(\mathbb Q(\sqrt{-3})\) and
\(\mathbb Q(\sqrt{-555})\). The completed B-closure count is again 51.

The new genuine Engine-B battery passed 3/3 B anchors, and a fresh
theorem-level reproduction passed 7/7. The 241-row recovery is active;
the 252-row C catch-up and 8,200-row genuine index ledger remain
subsequent gates. The historical norm trend is
`PROVISIONAL_WITHDRAWN`.

The paper has forked. `paper/effective-stark-results-paper.md` contains
only proxy-clean theorems and is scope-sealed independently of census
counts. The census paper is withheld pending v5.

## Banked headlines

The first generic Engine-C W3 closure is `VERIFIED`.

- Field: \(\mathbb Q(\sqrt{35})\).
- Primitive member: RQ-001280, finite norm 32.
- Exact transported member: RQ-001297, finite norm 64.
- Independent CM bases: \(\mathbb Q(\sqrt{-10})\) and
  \(\mathbb Q(\sqrt{-14})\).
- Both routes: \(e=2\), \(|S|=3\).
- Isolated anti-unit orbit:
  \(\{(-1,-1),(-1,1),(1,-1),(1,1)\}\).
- Common Artin-labeled packet polynomial:
  \(x^8-38904x^7+905404x^6+62136x^5-873210x^4+
  62136x^3+905404x^2-38904x+1\).
- Seal:
  `artifacts/engine-c-w3-tranche-01-verified-v1.json`.

The first elevated \(e=6\) generic tranche is also `VERIFIED`:
RQ-001569, RQ-007519, and RQ-001894 give three fields, six
independent imaginary-base routes, and 14 census occurrences.

RQ-000129 over \(\mathbb Q(\sqrt6)\) is now `VERIFIED`. Exact
auxiliary-prime enlargements at \(q=3,5\) have multipliers \(1+i\)
and \(2\); both routes normalize to the same primitive target, and
the \(q=5\) doubled orbit recovers the natural positive packet by its
unique positive square root.

## Cycles 061–065

1. Cycle 061 froze the generic W3 contract.
2. Cycle 062 implemented exact exhaustive CM character selection.
3. Cycle 063 implemented a generic certified theta/Mellin evaluator
   with explicit coefficient tails.
4. Cycle 064 implemented exact anti-unit lattices and Arb orbit
   isolation.  The Paper-II anchor caught and resolved the distinction
   between the class-log \(e/2\) factor and direct-\(L'\) \(e/4\)
   Fourier inversion.
5. Cycle 065 implemented the exact Artin/Frobenius normal-closure
   bridge, replayed the Paper-II anchor, and sealed both census
   members.

## Engine-D anchor gate and census containment

The proposed Engine-D anchor gate did not pass. It found a missing
hypothesis: the maximal-absolute-abelian index calculation requires a
finite modulus fixed by base conjugation. All 276 proposed substantive
Engine-D rows have unstable finite moduli. The corrected new Engine-D
population is zero, and the parity theorem proves that Dirichlet
characters cannot supply the asymmetric one-place characters.

The same correction quarantines 64 of the 195 nominal Engine-B passes.
The remaining 131 have stable moduli. All 177 former
`NO_ABELIAN_IMAGINARY_BASE` results are also unstable and withdrawn,
so the genuine B recovery population is 241. Engine A is unaffected.
The complete Engine-C gate constructs the actual splitting closure, so
its existing 728 positive cases remain valid; completeness requires a
catch-up run on 252 supported quartic rows excluded by the W1 proxy.
RQ-000458, RQ-000129, and RQ-002057 pass the new stability gate.

All 88 odd-index-proxy rows have unstable finite moduli. The measured
85/88 and 86/88 correlations are preserved as exact process
statistics but retracted as Shintani-index/normal-closure laws. Five
named exception files are banked.

## Results-paper referee repair and freeze v3

The results paper has passed a second, expanded referee audit and is
frozen at `artifacts/results-paper-freeze-v3.json`.  Its introduction
now displays the complete eight-item theorem inventory with separate
mathematical, historical, and process statuses.  The uniform Engine-A
formula is Theorem 1 and is explicitly identified as the paper's
broadest result, independent of the corpus and its software.

The \(\mathbb Q(\sqrt6)\) red flag was genuine: the earlier polynomial
had no real roots because it described the complex anti-unit before
unit-lattice division and CM norm.  Both \(e=8\) and \(e=12\) routes
now give the positive packet polynomial
\[
x^8-8x^7+12x^6+8x^5-10x^4+8x^3+12x^2-8x+1,
\]
with four positive roots and no negative roots.  The correction and
failed interpretation are both preserved.

The \(\mathbb Q(\sqrt{35})\) class-number assertion has a separate
exact PARI/GP 2.15.4 replay: the real base has class number 2 and the
two imaginary bases have class numbers 2 and 4.  The full 114-test
suite passes.  Two independent PDF builds are byte-identical.  Zenodo
DOI `10.5281/zenodo.21703306` is reserved but remains unpublished;
publication still requires explicit user approval.

### Freeze v4 clarification

The referee clarification pass is banked in
`artifacts/results-paper-freeze-v4.json`.  The introduction now fixes
\(\infty_2\) as \(\sqrt D\mapsto-\sqrt D\), PARI's first real place.
The theorem inventory has ten entries, adding the index-parity lemma
and its 446/446 replay together with the banked general-\(e\)
Engine-C normalization/orientation theorem for \(e=6,8,12\).  The
proof explicitly identifies the relevant fixed field as
\(H\cap N^{[G,G]}=H\cap\mathbb Q^{\rm ab}\).

Engine A now includes the closed universal formula
\[
X_A=\prod_{\chi(R)=-1}|\tau_1u_\chi|^{c_{A,\chi}},
\qquad
c_{A,\chi}=
\frac4{|G|}\chi(A)^{-1}E_\chi
\frac{h_{L_\chi}}{h_K}\frac{w_K}{w_{L_\chi}}\frac1{I_\chi}.
\]
The accompanying remark explains that a case reduces this expression
by substituting a finite list of exact inputs.

## W4 findings and next gate

The generic W3 architecture now exists and its \(e=6\) control is
banked. The next research block should:

1. parameterize the remaining field-polynomial/conductor inputs from
   the exact inventory rather than hand-written three-record configs;
2. begin the \(e=2,4\) and remaining \(e=6\) C-bulk;
3. reconstruct actual normal closures for all 241 unstable-modulus B
   screens, then rerun the B predicate;
4. run complete C geometry on the 252 proxy-excluded quartic rows;
5. rebuild the full 8,200-row index ledger from genuine normal
   closures before opening W4;
6. continue the stable 131-row B-bulk and existing A/C-positive bulks.

The rejected 276-case Engine-D split and all three negative anchors
are preserved as replayable process artifacts. No analytic or theorem
case-level theorem claim was promoted before the anchor gate fired.
One intermediate `VERIFIED_W2_*` classification family is explicitly
superseded; among 51 completed closure certificates, RQ-007500 is the
single affected canonical representative.
