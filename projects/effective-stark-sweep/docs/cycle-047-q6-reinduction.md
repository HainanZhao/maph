# Cycle 047 — Q(sqrt(6)) linear reinduction

Exact restriction on the normal closure produces two inverse matching
characters over each imaginary base.  Both character fields,
conductors, ray subgroups, and inverse dual pairs are printed.  The
source character is separated from its inverse by the exact
coefficient at \(n=3\).
