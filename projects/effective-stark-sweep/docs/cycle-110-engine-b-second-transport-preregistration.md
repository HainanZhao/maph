# Cycle 110 — second Engine-B transport target

After the sealed B5-015 transport, the next deterministic target is
the least-norm noncanonical member above a direct certified source in
the next such closure by frozen closure identifier:

| source | target | closure | base | source finite ideal | target finite ideal |
|---|---|---|---|---|---|
| RQ-000190 | RQ-000195 | B5-025 | Q(sqrt(7)) | `[[7,0],[0,1]]`, norm 7 | `[[14,7],[0,1]]`, norm 14 |

The first stage is restricted to the exact finite-modulus quotient and
canonical ray-class map, including identity, generator, and sign
labels.  Only if that map is an isomorphism and the quotient is a
single prime ideal may the Cycle-108 Euler-deletion distribution proof
be instantiated.  Otherwise this target is recorded as a failure or
different transport geometry; no packet equality is assumed.

The previously frozen 600-second and 2-GiB cap applies.  The direct
RQ-000190 Arb certificate must be replayed under Python 3.12.3 and
python-flint 0.9.0 before any target transport certificate is issued.
