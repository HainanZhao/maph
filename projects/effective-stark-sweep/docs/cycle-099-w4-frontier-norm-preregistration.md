# Cycle 099 — W4 frontier share by conductor norm

The frozen bins are finite ideal norm 1--25, 26--50, 51--75, and
76--100.  Join the genuine-v5 verdict records to the independently
enumerated finite norms by RQ identifier, count `FRONTIER` rows in each
bin, and require exact agreement with the four fractions stored in the
v5 yield declaration.

The output is an `OBSERVED` finite-range trend.  It is not an
asymptotic-density result, causal claim, or prediction beyond the
frozen range.  A disagreement blocks W4's later slices.
