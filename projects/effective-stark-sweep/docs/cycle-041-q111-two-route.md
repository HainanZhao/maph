# Cycle 041 — the Q(sqrt(111)), norm-3 case

`RQ-004467` is the smallest new degree-16 B closure.  Route 1 finds
\(\mathbb Q(i)\) and \(\mathbb Q(\sqrt{-111})\); both exact Route-2 ray
subfields reconstruct the same normal closure.  The \(\mathbb Q(i)\)
route has the shorter divisor table.
