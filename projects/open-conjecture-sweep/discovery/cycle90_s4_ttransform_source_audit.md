# C90 source and convention audit: S4 T-transform

## Source interfaces

`PROVED` from Lee--Schülke, *Convex graphon parameters and graph norms*,
Theorem 1.3: \(K_{5,5}\setminus C_{10}\) is not weakly norming, while its
Sidorenko status is stated there as undetermined.  This does not settle a
Cayley T-transform comparison.

`PROVED` from Blekherman--Raymond--Singh--Thomas, Corollary 1.6:
\(H-e^{15}\) has neither an SOS nor an \((1+\mathrm{SOS})\)-multiplier SOS
certificate.  A class-coordinate transfer is a different proposed proof
interface.

`PROVED` from Zhao, arXiv:2606.15368v1, Theorem 1.3: the comparison
\(t_{\rm Cay}(H;\Gamma,a)\ge t_{\rm Cay}(H;\Gamma,a^{\rm cl})\) for every
finite group and nonnegative \(a\) implies strong Sidorenko.  The planned S4
line is one necessary consistency test for that universal premise, not an
instance of its conclusion.

`PROVED` from C68's sealed record: every nonnegative \(a:S_3\to\mathbb R\)
satisfies the fixed-host comparison.  C68 expressly forbids an S4/S5
**census** but authorizes a representation-theoretic or
conditional-expectation lift.  C90 fixes one predicted T-transform and
requires a character factorization; it is not a census.

`OBSERVED`: a bounded exact-title/arXiv/current-primary search found no source
stating this particular S4 two-coordinate T-transform identity.  This is not
a novelty claim.  Official OpenAI searches for the exact target names returned
no matching announcement; this is also only `OBSERVED` eligibility evidence.

## Conventions to verify before execution

Use the labelled Möbius graph with left/right \(\mathbb Z_5\) and edges
\((i,j)\) for \(i\in\{j,j+1,j+2\}\).  For a finite group \(\Gamma\) and
\(a:\Gamma\to\mathbb R_{\ge0}\), the direct route must pin Zhao's Cayley
normalization and all tuple/product orientations from the theorem statement.
The independent route must construct the same class-algebra multiplication
table from the S4 permutation model and separately verify its character table.

## Claim boundary

All C90 attack claims remain `CONJECTURED` until a preregistered exact replay.
This audit proves only the source scopes quoted above.
