# C91 selection: two-cut blocking-ideal injection

Oracle selects, conditionally on source clearance, one substitution-prime
Gold-obstructed poset, one majority-oriented incomparable pair, and two
predeclared ideal-lattice cuts. The test is a deterministic prefix/suffix
splicing injection from majority extensions into two tagged minority copies.

The exact falsifier is a missing image, invalid image, equal-tag collision, or
independent ideal-DP/enumeration mismatch. A finite pass has no theorem value
unless it yields a source-clear cut-profile lemma for a nontrivial
substitution-prime class. Do not repair cuts or enlarge a census after failure.

The main rejected Ryser trace gate is too weakly linked to a hypothetical
\(\tau=6\) core. The central risk is overlap with Gold/Peczarski certificates;
that primary-source audit is mandatory before C91 preregistration or code.
