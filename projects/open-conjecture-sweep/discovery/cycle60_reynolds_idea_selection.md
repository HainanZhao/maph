# C60 idea selection: all-at-once Reynolds centralization

Write (a=c+b), with (c=P_{\rm cl}a) and (b) in the kernel of class
averaging. Unlike the failed one-class routes, target only the endpoint
\(N(c+b)\ge N(c)\). The chosen engine decomposes the full deficit under the
simultaneous conjugation action into isotypic blocks; a PSD block identity
would prove the directly relevant comparison. A non-PSD frozen block rejects
only that template, while a rational endpoint reversal rejects Zhao's route.
