# C91 two-cut overlap audit

`PROVED` from Peczarski's Gold Partition Conjecture statement: its two
consecutive comparisons require an inequality \(t_0\ge t_1+t_2\) in every
branch and imply the 1/3--2/3 conjecture.  A prefix/suffix map from majority
extensions into two tagged minority copies is precisely a proposed injection
proof of this same 1:2 branch inequality, not a distinct invariant.

`PROVED` from Gupta's 2026 abstract: every non-chain through order 14 is
assigned a Peczarski certificate.  Thus no small frozen control can distinguish
the proposed map from the already-certified Gold mechanism without first
specifying a new profile theorem.

`OBSERVED`: Jaldevik--Snellman's blocking-ideal framework supplies a general
counting representation, but the bounded read found no source-cleared reason
that the proposed two-cut injection escapes the Gold certificate language.

## Decision

The C91 conditional selection fails **before execution** for source overlap.
Do not preregister, code, or census this injection.  Return to portfolio
discovery and require a mechanism whose conclusion is not merely the Gold
two-comparison inequality.
