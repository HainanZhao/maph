LARGE VALUE ESTIMATES IN NUMBER THEORY, HARMONIC ANALYSIS, AND COMPUTER SCIENCE 27

PDirA
&1
!

We can write

(28)
ρ1/T,Dir = ρsmooth + ρspikes.

Here ρsmooth is a smooth function of height ∼ N 2 like above. The function ρspikes is quite large
near to log(p/q), where p, q are small integers, but it is essentially zero away from those points. The
spikes occur at log p/q where |p|, |q| ⪅ T/N and each spike has width 1/T. So if T is, for example,
N 6/5, we see a fairly sparse set of tall spikes. The height of a typical spike is ∼ N 2, although the
spikes corresponding to small p, q are taller.
The sets DrΦDir for larger r behave in a similar way. For instance, if r = 3, then D3Φ is a subset
of a 2-dimensional disk of radius ∼ 1. If we take its density at scale 1/T, then we get a smooth
part plus spikes along a small number of curves. Each curve corresponds to a rational point of the
form ( p1

p3 , p2

p3 ), with |pi| ⪅ T/N, and so the number of curves is ⪅ (T/N)3. The spike over a typical
curve has height ∼ N 2 over a 1/T-neighborhood of the curve.
These plots of densities are closely related to estimating the singular values of MΦ,W . Recall
that we have (24)

�

i
si(MΦ,W )2r =
�

(τ1,...,τr)∈DrΦ
ˆW(τ1) ˆW(τ2)... ˆW(τr)

Since W is contained in [0, T], ˆW is morally roughly constant at scale 1/T, and so this sum is
approximately an integral with the density at scale 1/T: �

i si(MDir,W )2r is approximately

(29)
�
ˆW(τ1) ˆW(τ2)... ˆW(τr)ρ1/T,Dir,

which we can break up as


