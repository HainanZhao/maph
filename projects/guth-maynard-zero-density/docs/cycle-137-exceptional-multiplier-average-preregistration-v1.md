# Cycle 137 preregistration: exceptional-multiplier average

Date frozen: 2026-08-02 UTC.

In a strict Cycle-136 block, dyadically restrict the fixed-difference edge
multiplicity to `|E_d|~J=X^j`.  Count differences `d` for which `g^d` lies
within `O(N^2/S^2)` of a rational of height at most `N^2`, retaining both the
mode-grid volume term and the rational-discretization term.

Compare that exceptional count with the diagonal edge-weight allowance
`(Q/M)^2/J^2`.  Solve the resulting exponent inequalities exactly and record
one explicit nonempty closed cell.  If the full region does not close, name
the dominant term and preserve the weighted convergent graph as the inverse
output.  Do not replace the edge-weight target by an unweighted count.
