# Cycle 144 preregistration: actual edge-coefficient bridge

Date frozen: 2026-08-02 UTC.

Trace the coefficient data, without taking absolute values, from the
Cycle-123 joint radial alias through the Cycle-124 tensor separation and the
Cycle-132--136 collision inverse.  Keep visible the stationary signature,
all smooth cutoffs, tensor frequencies, edge orientation, and rational-tail
labels.

First determine whether the sealed records define a coefficient-preserving
map from the alias weights `w_alpha(a,n;ell)` to the scalar edge weights
`w_a` in the paired norm.  If they do, write that map and compute the exact
zeroth and first path moments.  If they do not, identify the first missing
map as a typed interface rather than silently equating the two vectors.

Separately test the leading Cycle-123 smooth coefficient on a fixed interior
chart.  Record whether its phase/sign forces cancellation or permits a
coherent local block.  This local test is not a saturator for the full
operator unless the coefficient-preserving inverse map is proved.

Success is one of:

1. a proved vanishing or power-saving identity for the actual `M_0` or
   `M_1` after a coefficient-faithful bridge;
2. a coefficient-faithful one-sign block showing that the zeroth moment can
   saturate; or
3. a precise interface theorem proving that the existing inverse output is
   coefficient-blind and specifying the minimal signed measure that the next
   engine must transport.

No outcome may be promoted to a full paired norm, complete moment, density
gain, or prime-interval theorem without the corresponding replayed estimate.
