# Cycle 187: separated weighted-packing limit

## Claim boundary

`PROVED`: critical ordered cross mass, complete-fibre capacity, stable shells,
and a C186-scale local triple exclusion cannot alone force a strict
populated-box saving. An explicit support has at most one label in every
window much larger than the C186 local scale while retaining the critical
`X^(21/25)` ordered mass.

This is an abstract occupancy model, not an actual positive-exponential
configuration. It proves no analytic counterexample, box saturation theorem,
recurrence, density gain, or interval result.

## Exact separated ledger

Put `T=3^(2k)`, so

```text
X=T^25, H=T^11, Delta=T^15, S=T^2, U=T^9, M=3^(13k).
```

Select the first `M` labels in

```text
1 + T^2 * {ternary integers with 24k digits, each 0 or 1}.  (1)
```

There are at least `M` choices, (1) lies below `Delta`, and distinct selected
labels differ by at least `T^2`, stronger than the C186 `T`-scale local
exclusion. Give each label a complete abstract fibre of depth `S+1`. Then

```text
M*(M-1)*binom(S+1,2)^2 >= X^(21/25)/8,                  (2)
```

and `U^2` exceeds the stable cutoff.

## Gate effect

Cycle 186 is a real actual-curve local theorem, but Cycle 187 proves that
its local packing content cannot reach E13 using the existing weighted
ledger alone. A future continuation needs a genuinely global distribution
law for actual exponential approximants across separated labels or denominator
windows. The next remaining cycle should capture that precise restart
interface rather than run another local classifier.

