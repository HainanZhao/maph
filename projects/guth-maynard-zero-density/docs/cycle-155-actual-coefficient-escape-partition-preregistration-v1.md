# Cycle 155 preregistration: actual coefficient-escape partition or complexity inverse

Date frozen: 2026-08-02 UTC.

## Objective

Starting from the exact Cycle-153 escape component `F`, construct or reject an
exact additive decomposition

```text
F = sum_(j in J) F_j
```

into disjoint, reason-labelled coefficient-escape classes. The decomposition
must be derived from the actual coefficient object, rather than inferred from
local fixed-sign charts or a scalar collision model.

## Frozen interface and success criterion

Every summand retains its original coefficient, frequency, phase-anchor,
degree, rational-tail, and payload labels. Its class has one registered
reason: coefficient-transport failure, boundary denominator, phase-changing
chart, nonsmooth payload, unbounded tail, failed rational label, inadmissible
lcm, registered truncation, or explicit tensor remainder.

Success requires all of the following.

1. The sum is exact, disjoint, and exhaustive for the actual `F` of Cycle
   153.
2. The number of reason classes is a frozen `J=O_kappa(1)` independent of the
   asymptotic parameter; labels remain available within each class.
3. Every discarded/regularized term is assigned a registered class, including
   every Riemann, Poisson, stationary-phase, chart, tensor, and truncation
   error.
4. If the Cycle-153 escape projection is at least `kappa`, invoke Cycle 154
   to localize an actual class with normalized projection at least `kappa/J`.
   State the one-ray `L2` consequence only after separately certifying a fixed
   comb-norm bound `||w_h||_2^2<=A W_h`.

An `X^(o(1))` number of classes, a classifier missing a term, a class whose
retained labels are discarded, or an informal ``remainder'' is failure, not
success.

## Registered failure output

If a fixed `J` cannot be obtained, freeze a quantitative coefficient-
complexity/entropy inverse: identify which of chart, degree, tensor rank,
frequency-anchor, or tail-label complexity grows; give the exact growth
parameter and an additive labelled decomposition at that complexity. It may
motivate a new inverse engine, but it does not activate Cycle 154's fixed
class conclusion.

## Companion checkpoint

The persistent companion `/root/guth_maynard_session_mentor` was reactivated
under its stable identity and reviewed the sealed Cycle-154 conditional
localization result on 2026-08-02 UTC. It recommends this partition-or-
complexity-inverse search, with E14D-L remaining open. The primary adopts the
recommendation: Cycle 154 is recorded only as a conditional compiler; neither
an actual escape class nor positive transport is claimed.
