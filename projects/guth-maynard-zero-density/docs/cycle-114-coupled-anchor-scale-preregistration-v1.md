# Cycle 114 preregistration: coupled anchor-scale-label sum

Date frozen: 2026-08-02 UTC.

Retain simultaneously `B=p0*n'=lambda*B0<=Q`,
`C=q0*m=lambda*C0<=Q`, the fixed support `n,n',m~Q`, and the corrected full
kernel `1/sqrt(mnn')`. Prove the resulting anchor-height bound and scale
window before summing primitive splits. The split sum must use the exact
identity `gcd(t,R)=sum_(a|t,a|R)phi(a)` and retain the `+1` residue-class
term. Success closes only the registered smooth strong near-double branch;
failure freezes an exact supported saturator.
