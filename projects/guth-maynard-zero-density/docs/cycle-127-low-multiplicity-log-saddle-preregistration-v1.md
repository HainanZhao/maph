# Cycle 127 preregistration: low-multiplicity logarithmic saddle

Date frozen: 2026-08-02 UTC.

On a dyadic Cycle-92 multiplicity class `M=X^mu`, put `L=Q/M`. Rewrite the
occupied primitive labels as lattice points

```text
|a-(D/(2pi))log(p/q)| << D/(KQ),  p,q~L.
```

Apply the checked order-three Huxley--Sargos theorem in `p` for each `q`, sum
all four terms, reinsert multiplicity `M`, and compare every exponent with
the collision target `Q`. Separately compute the two-dimensional volume term.

Then derive the exact sampled Mellin second-moment estimate which would give
volume-scale counting jointly in `(p,q)`. Success is a closed nonempty region
or a precise major-arc/large-sieve contract with its exponent deficit. Do not
claim that failure of the one-dimensional theorem excludes a joint method.
