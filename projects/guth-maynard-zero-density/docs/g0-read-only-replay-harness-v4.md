# G0 read-only replay harness v4

## Outcome

`OBSERVED`: [v4](../proof/run_g0_replay_v4.py) is the minimal hardened
successor. It first calls runtime convention v2, which explicitly requires
non-optimized CPython 3.12.3, then replays the complete v3 inventory and
checks the optimization-robust authoritative
[`g0-full-reconstruction-v3`](../artifacts/g0-full-reconstruction-v3.json).

`OBSERVED`: a `python3 -O` or `python3 -OO` invocation fails before either
the inherited v3 harness or the v3 certificate runs. The older v2 optimized
mode bypass remains recorded in the bounded hostile audit; v4 does not erase
or reclassify it.

`OBSERVED`: this is operational replay only. It does not prove a new
zero-density estimate, a new short-interval exponent, or a new G0 decision.

## Replay and regression

```sh
python3 proof/run_g0_replay_v4.py
python3 -m unittest tests/test_g0_replay_harness_v4.py -v
```

The direct v4 inventory has exactly two read-only commands: v3 and
`reconcile_g0_full_v3.py --check`. Its fixed hashes bind those two scripts,
the v3 artifact, and runtime convention v2. Host-specific resource evidence
remains `OBSERVED` and is never regenerated by the runner. The separately
sealed `artifacts/g0-read-only-replay-v4-performance-v1.json` records a
successful 29.27-second, 52608-KiB replay on the stated host/runtime; those
figures are performance observations, not mathematical claims.
