# Cycle 136 preregistration: common-multiplier scalar compiler

Date frozen: 2026-08-02 UTC.

Work in the strict Cycle-133 region `S>>N^3` and fix a nonzero represented
mode difference `d`.  Use exact multiplicative rectangles to prove that all
edges `a -> a+d` share one reduced rational multiplier `r_d`.

Factor the Cycle-135 paired residual through `r_d-g^d`.  Apply the elementary
large sieve to the separated rational labels `x_a`, recording the precise
scalar threshold at which the diagonal paired-tail second moment follows.

If the scalar threshold fails, compare the rational height of `r_d` with its
approximation error to `g^d`.  Apply Legendre's criterion only where its
hypothesis has a fixed-power margin, and derive the next denominator and next
partial-quotient lower bounds.  Success is a proved diagonal/continued-
fraction dichotomy, not closure of the exceptional multipliers averaged over
`d`.
