# Cycle 142 preregistration: changing-color valuation walk

Date frozen: 2026-08-02 UTC.

Along a fixed nonzero rational multiplier `A/B`, derive the exact reduction
factor and prime-valuation update for successive reduced labels.  Treat the
cross-gcd color as a deterministic state variable, not an independent edge
label.

Reapply the frozen rational-geometric height argument at label height `N` to
bound every supported chain.  Combine that bound with the exact path-forest
edge/depth inequality and compute the edge density needed to force a
contradiction.  Success may be a scoped saturation theorem showing that this
recurrence compiler cannot yield a fixed-power gain.  State the analytic
sparse-component object that remains; do not promote logarithmic depth to a
packet seed.
