# Cycle 2 — Stream C hostile two-route reconciliation v2

Claim boundary: `PROVED` only for the narrow independent reconciliation of
sealed Routes A v5 and B v5, conditional on Guth--Maynard’s published
zero-density theorem. It proves neither that density theorem nor a new
short-interval result, and it does not establish G0 PASS. Reconciliation v1
is preserved.

`PROVED`: all 26 preregistered labels have zero gaps. The map covers the
formula theorem/range/remainder/endpoints/multiplicity/height bridge; Huxley,
Vinogradov--Korobov, and local-pair inputs; and uniform and almost-all
truncation, epsilon, range, error, exceptional-set, and prime-conversion
steps. The exact shared values are (B=30/13), uniform
(	heta=17/30), and almost-all (	heta=2/15).

`PROVED`: Route A and Route B pin identical hashes for source-closure v4, its
checker, the independent SWORD audit, official ZIP and PDF members, DSpace
metadata, and Guth--Maynard TeX. The independent SWORD audit retains its own
`OBSERVED` provenance status; it is corroboration, not a silent status upgrade.

`PROVED`: timing-mutable Route-A v1--v3 inputs are represented only by their
canonical semantic identities checked by Route A v5. No raw hash of their
timing-dependent artifact bytes occurs in v2. Route-B v2 contributes only a
canonical arithmetic projection; its superseded formula-source provenance is
not imported.

The narrow result is a Stream-C independent pass only. It does not promote a
new density exponent, a new prime-interval exponent, or G0.

## Replay

```sh
python3 projects/guth-maynard-zero-density/proof/reconcile_cycle2_stream_c_two_routes_v2.py --check
```
