# Cycle 130 preregistration: broad continued-fraction cylinders

Date frozen: 2026-08-02 UTC.

For a Cycle-129 multiplicity class, put

```text
A0=KM^2/Q,  q0=(D/A0)^(1/2),  L=Q/M.
```

Cover every collision convergent with `q<=q0` and next partial quotient
`>>A0` by an interval of length `O(1/(A0q^2))` about `p/q`. Use the exact
spacing `asymp 1/D` of the compact exponential mode grid to bound each
interval by `1+D*length`, and sum both the component and length terms.

Freeze the exponent margins showing `1<<q0<<L` throughout the registered
low-multiplicity range. Success is closure of `q<=q0` and an explicit narrow-
cylinder complement; it is not closure of all low multiplicities.
