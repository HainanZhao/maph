# Cycle 112 preregistration: corrected full triple-B symbol

Date frozen: 2026-08-02 UTC.

Starting from the paired Cycle-81 amplitude `c^2/(r r')`, substitute all
three corrected stationary points before bounding. Freeze every `W,V`
argument. Prove or refute that the resulting symbol has no multiplicative
`c0` loss and that support forces the actual scale to absorb the rational
anchor height. Success must combine with Cycle 110 to give arithmetic
multiplicity `M^(1+o(1))`, strictly below `Q M^(1/2)=X^(19/30+o(1))` after
the common analytic prefactor is removed. No claim about nonsmooth payloads
or other root branches is authorized.
