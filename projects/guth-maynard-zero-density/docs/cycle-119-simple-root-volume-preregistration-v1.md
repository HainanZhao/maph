# Cycle 119 preregistration: simple-root volume barrier

Date frozen: 2026-08-02 UTC.

For the Cycle-118 simple-root model, compute the zeroth Fourier mode of a
Selberg majorant for

```text
dist(B exp(2 pi a/D)+C exp(2 pi b/D), Z) <= c/K,
```

with `B,C~Q` and `|a|,|b|<<D`. Insert the corrected `Q^(-3/2)` kernel and
compare its exponent with the Cycle-114 benchmark `13/30` throughout
`16/25<=xi<58/75`.  State the resulting limitation only for
positivity-preserving or termwise-absolute Fourier estimates; do not promote
it to a theorem excluding all unsigned discrepancy arguments.  Preserve the
same-sign/opposite-sign split in the nonzero Fourier factorization and output
the exact power saving that a signed/discrepancy estimate must supply.
