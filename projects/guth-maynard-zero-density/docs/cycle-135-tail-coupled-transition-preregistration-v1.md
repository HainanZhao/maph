# Cycle 135 preregistration: tail-coupled transition operator

Date frozen: 2026-08-02 UTC.

Write the Cycle-134 shell using
`Y=1/(q|g^a-p/q|)=R+theta*q`.  First sum the shell indicators over a complete
contiguous shear block `R=r+tq` and determine whether Fourier projection in
`theta` alone produces any saving beyond Cycle 132.

Then, for a fixed mode difference `d`, combine the exact decompositions of
`g^a` and `g^(a+d)` to derive the signed two-tail edge identity.  Normalize
the edge residual at scale `NS` and state the phase-coupled finite norm that
would have to replace the marginal tail projection.

Success is either a strict transition-entropy saving or a scoped proof that
tail-only projection is self-dual, together with an exact paired-tail target.
No conclusion about the paired operator may be inferred from failure of its
marginal.
