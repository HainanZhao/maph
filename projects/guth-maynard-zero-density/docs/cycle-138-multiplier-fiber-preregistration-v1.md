# Cycle 138 preregistration: multiplier-fiber height descent

Date frozen: 2026-08-02 UTC.

For a reduced compact multiplier `r=A/B` of height `H`, parameterize every
primitive compact rational `x=p/q` of height `N` for which `rx` also has
height `N`.  Use the exact cancellation divisor
`gcd(Ap,Bq)=gcd(A,q)gcd(B,p)` and sum over its divisor classes.

Convert an edge multiplicity `|E_d|~J` into a height bound for `r_d`, then
repeat the Cycle-137 exceptional interval count with that reduced height.
Compare the result after multiplication by `J^2` with `(Q/M)^2`, solve both
exponent inequalities, and determine the width beyond the Cycle-131
denominator ceiling.  Success closes all edge multiplicities only for the
proved regional average; it is not full endpoint closure.
