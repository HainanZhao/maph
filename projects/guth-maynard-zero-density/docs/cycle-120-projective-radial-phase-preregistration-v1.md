# Cycle 120 preregistration: projective radial phase

Date frozen: 2026-08-02 UTC.

Return to the exact Cycle-94 phase `cF(H,Delta)-uH-vDelta`, with
`c=D/(2pi)`, before any absolute value.  Put `Delta=zH`, derive its exact
homogeneous normal form `H P_(u,v)(z)`, solve the projective stationary
equation, and express the surviving radial frequency in terms of the
Cycle-95 Laurent residual

```text
R=A-Bg^u-Cg^(u+v),  g=exp(2pi/D).
```

Freeze the sign and two-sided comparison between the radial frequency and
`cR/A` on `|R|<=A/2`.  Compare radial coherence at height
`H0~KQ/D` with the Cycle-116 tolerance `1/K`.  Success is an exact
phase-carrying normal form that identifies the signed kernel to be estimated;
it is not a simple-root bound or moment closure.
