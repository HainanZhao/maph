# Cycle 143 preregistration: sparse-path Fourier compiler

Date frozen: 2026-08-02 UTC.

Decompose one exceptional fixed-difference graph into the logarithmic paths
from Cycle 142.  Layer paths by their position and apply the separated-label
large sieve to each layer with arbitrary complex weights.  Include the exact
Cauchy loss across layers and determine whether it changes the Cycle-136
scalar threshold at power scale.

If the threshold is unchanged, exhibit the scoped coherent-weight saturator
and expand the exceptional small-scalar phase in signed coefficient moments.
Name the first coefficient-sensitive invariant which is absent from the
arbitrary-weight architecture.  Do not infer failure for the actual signed
weights.
