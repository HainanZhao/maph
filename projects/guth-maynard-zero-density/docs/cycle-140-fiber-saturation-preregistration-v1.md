# Cycle 140 preregistration: multiplier-fiber saturation inverse

Date frozen: 2026-08-02 UTC.

Refine the Cycle-138 height bound by writing the actual compact multiplier
height as `H=N^2/(JZ)`, `Z=X^zeta>=1`.  Recompute the exceptional rational
count, multiply by `J^2`, and solve the two strict closure inequalities for
`zeta`.

For a surviving block, recompute the Legendre margin, next denominator, and
next partial quotient using the actual height `H`.  Then return to the exact
cross-gcd decomposition of the multiplier fiber and pigeonhole one divisor
class carrying a subpower proportion of its edges.  Record the resulting
divisibilities and reduced labels explicitly.  Success is a saturation
inverse with a phase-labelled divisor seed, not recurrence or endpoint
closure.
