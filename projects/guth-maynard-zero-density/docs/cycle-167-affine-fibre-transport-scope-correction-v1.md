# Cycle 167 scope correction v1

## Correction

The top-level `claim_boundary` of
`artifacts/cycle-167-affine-fibre-transport-v1.json` says “one-step affine
multiplicative architecture.”  That wording is broader than the frozen theorem
record, which is only about the **reduced rational approximant ansatz**
`B=q/a`, with `(a,q)=1`.

The corrected boundary is:

> Within the one-step reduced-rational affine multiplicative architecture,
> this classifies beta-preserving cross-label edges. It proves no eligible
> actual Cycle-166 fibre, target-local packet, recurrence, E7/E9 skeleton,
> density, or interval gain.

No identity, convention, finite test, parent-count result, or mathematical
conclusion changes. In particular, arbitrary real multipliers such as
`B=1/E` were never proved to be classified by Cycle 167 and lie outside the
result. The original artifact remains immutable; this correction is its
authoritative claim-boundary amendment.
