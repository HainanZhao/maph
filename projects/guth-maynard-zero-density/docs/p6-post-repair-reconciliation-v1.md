# P6 post-repair reconciliation v1

## Outcome

`OBSERVED`: the original immutable 46-row reconciliation is now overlaid with the narrow repairs. The local multiplicity, low-height, polynomial-growth, primitive fourth-moment, conductor-transfer, `q1` reset, and amended detector-tail components are supplied in their stated scopes.

`CONJECTURED`: the general all-character `7/3` envelope is not promoted. The primitive CGL-style large-value/two-term envelope, several comparator and energy inputs, and the full middle-range assembly remain unproved in this project.

## What is now supplied

`PROVED`: v2 supplies `L_POLY_A`; v3 supplies `LC` and hence the v1 low-height count; v4 supplies the `q >= 2` primitive discrete fourth moment. The primitive-to-all artifact supplies zero-multiset equality and the conductor partition. The `q1` reset proves the correct conductor-level transfer once a primitive monotone envelope exists. The detector tail is `PROVED` for its amended cutoff and the primitive class-II step is `PROVED` in the repaired `q >= 2` scope.

`PROVED` with stated hypotheses: the corrected `T`-smooth divisor chain and smooth `(30/13)` branch hold under the amended definition and the retained primitive large-value/comparison assumptions. F09 and the conditional `7/3` domination are exact algebra.

## What remains open

`CONJECTURED`: no checked P6 source closes the low-value comparator, HMH comparator, S2 approximate-functional-equation input, character-time energy input, Dirichlet Ingham/Huxley outer inputs, or the central primitive CGL-style large-value envelope. Therefore the full F03/F10 assembly and a general `7/3` theorem remain open.

`OBSERVED`: CGL's own multiplicity wording, domain/tail wording, saturated selection construction, undefined `T`-smooth terminology, and prescribed-`q1` intermediate formulae remain source-text gaps. The repairs do not rewrite the preprint.

## q=1

`PROVED`: `q=1` is separately covered by the pinned G0 Guth--Maynard zeta route at the published `30/13` envelope. It is not an all-character P6 `7/3` reconstruction and does not enlarge v4's intentionally `q >= 2` scope.

## Replay

```sh
cd /root/projects/maph/projects/guth-maynard-zero-density
python3 proof/build_p6_post_repair_reconciliation_v1.py --check
python3 -m unittest tests/test_p6_post_repair_reconciliation_v1.py -v
```
