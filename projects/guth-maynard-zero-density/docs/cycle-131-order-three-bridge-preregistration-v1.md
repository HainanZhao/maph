# Cycle 131 preregistration: order-three denominator bridge

Date frozen: 2026-08-02 UTC.

On a low-multiplicity dyadic denominator block `q~X^rho`, specialize the
checked order-three Huxley--Sargos theorem directly to
`a=(D/(2pi))log(p/q)` at thickness `D/(KQ)`. Include the number of
denominators and the collision multiplicity `M=X^mu` before optimizing.

Solve the derivative-term equality with target `Q`, verify the tube, ratio,
and constant terms throughout the resulting range, and compare its ceiling
with both the Cycle-130 broad cutoff and the full `Q/M` ceiling. Success is a
strict extension of the closed denominator range and an exact remaining
endpoint width; it is not full low-multiplicity closure.
