# G0 full reconstruction v1

Claim boundary: `PROVED` only as an authoritative adjudication of the frozen
G0 reconstruction gate, conditional on the selected published analytic
theorems whose hypotheses were checked. This reconstructs published
Guth--Maynard results. It proves no new density estimate, short-interval
exponent, saturation theorem, or L-function extension.

## Decision

`PROVED`: **G0 PASS**, with no open blocker in the frozen G0 scope.

- Cycle 1: 24/24 exact labels agree across two implementations.
- Stream B: 7/7 application/source rows agree, with zero coverage gaps.
- Stream C: 26/26 formula, convention, uniform, and almost-all labels agree
  exactly across the sealed v5 routes.
- Source gate: all 15 inherited graph nodes are mapped and all 8 selected
  literature/source-hypothesis gates pass. Unread alternatives were removed
  from the selected path rather than silently accepted.
- Source inventory: 41 frozen items verify; the official MIT DSpace/SWORD
  formula chain replaces the superseded author-copy provenance premise.
- Independence: `OBSERVED` structural audit found no cross-route replay or
  artifact dependency before reconciliation.
- Resources: `OBSERVED` all four current route checks exited zero below the
  strict 60-second and 262144-KiB ceilings.

The matrix-v2 in-place refresh, historical timing-byte drift, formula-source
provenance correction, and stale Huxley title metadata remain preserved and
contained in the certificate. They are not erased by this decision.

G0 PASS authorizes P1 only after the atlas ranges, resolution, refinement,
families, seed, cap, failed-row rule, and retention margin are preregistered.

## Replay

```sh
python3 projects/guth-maynard-zero-density/proof/reconcile_g0_full_v1.py --check
```
