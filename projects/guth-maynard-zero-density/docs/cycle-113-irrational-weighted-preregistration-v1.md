# Cycle 113 preregistration: weighted irrational-core aggregation

Date frozen: 2026-08-02 UTC.

## Scope

For a reduced critical label `N/R`, primitive `u+v=d`, and
`x=(u,R)`, `y=(v,N)`, use the exact normalized weight

```text
J=(K B0 C0)^(-1/2),
K=(dR/(xy))*(N/R)^(u/d),
B0=vR/(xy), C0=uN/(xy).
```

The active labels lie on the frozen compact ratio chart. This cycle may
combine weighted split aggregation with the Cycle-104 norm separation; it
may not replace the latter by floating recognition.

## Gates

1. Derive and exact-test the general product formula.
2. Search `d<=120`, coprime `N,R<=120` for the largest values of the split
   sum and of `sqrt(d)` times that sum. This is a falsifier only.
3. Prove the strongest uniform weighted split bound supported by a divisor-
   class decomposition, retaining all endpoint losses.
4. Apply Cycle-104 separation on degrees for which its exact safe bound beats
   tolerance. Treat the complementary degrees by the proved weighted tail,
   not by a pointwise logarithmic-form claim.

Outcomes are `WEIGHTED_TAIL_CLOSED`, `POLYNOMIAL_DEGREE_LOSS`, or an explicit
`FROZEN_IRRATIONAL_SATURATOR`. No complete signed-moment or density promotion
is authorized.
