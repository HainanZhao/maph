# Cycle 149 preregistration: target-mass comb inverse

Date frozen: 2026-08-02 UTC.

Let `C` be the strict endpoint operator from Cycle 148, `R` the exact
complement on the same frequency block, and `F=C+R` the full fixed
polynomial.  Compare

```text
||C||_2^2 >> KQ^2 R_C/N
```

with the global diagonal budget `B0=KDQ`, retaining all constants as fixed
chart constants.

Prove the exact endpoint occupancy threshold and the Hilbert-space inverse:
if `(R_C/D)(Q/N)>=Lambda` but `||F||_2^2<=C_diag B0`, then `R` lies within
relative distance `sqrt(C_diag/Lambda)` of `-C`. In the ideal divisor-comb
model `C_comb(k)=Q sum_(h|k)A_h`, extract one denominator modulus whose multiples
have a quantified negative correlation with the complement.

Success is a coefficient-sensitive anti-alignment theorem retaining the
frequency block, denominator modulus, anchor, endpoint labels, and common
coefficient vector.  It is not an exclusion of anti-alignment, a full second
moment, density gain, or prime-interval theorem.
