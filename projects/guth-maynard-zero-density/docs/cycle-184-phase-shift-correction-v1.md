# Cycle 184 correction: positive-exponential slope is `z^j-1`

`PROVED`: Cycle 184's LCM resonance identity is unaffected, but its
nonrational deformation wrote `alpha_j=z^j` despite the pinned convention
`alpha_j=z^j-1`. Its original deformation wording is therefore superseded.

`PROVED`: replace the two rational ray slopes `q,q^2` by `q-1,q^2-1`, with
numerators `B-V,B^2-V^2`. The residual bounds are unchanged, and the primitive
determinant remains `V*B*(V-B)` because the common numerator shifts cancel.
The full sub-seed, stable, and subcritical-box calculation survives.

This correction proves no critical-box bound, recurrence, density gain, or
interval result.
