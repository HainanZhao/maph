# Cycle 40 hollow-notch preregistration v1

## Claim boundary

This cycle may prove an exact positive-kernel floor for the amplified
prime-monomial polynomial and use it to delimit global mean-value routes to
`AMPR_s`. It may not refute the hollow discrete estimate or promote a kernel,
density, or interval gain.

## Frozen objects

```text
F_(m,s)(t)=K(t)^s K(mt),
H=X^(12/5), Delta=X^(3/5), A=X^(3/10),
C subset {t:Delta<=|t|<=H}.
```

For fixed `s`, center the frequencies of `F_(m,s)` by the common phase
`X^(s+m)`. Freeze their bandwidth before applying any mean-value analogy.

## Registered tasks

1. Prove the triangular-kernel identity for the centered polynomial.
2. Partition its frequency interval into `1/H` bins and derive a rigorous
   lower bound using positivity and Cauchy--Schwarz.
3. Compare the resulting floor with `AMPR_3` and `AMPR_4`.
4. Decide only the scoped question: can an unmodified global positive-kernel
   mean value prove the hollow targets at their stated scale?
5. If not, define the next object as a hollow/notched restriction operator
   that removes the coherent zero neighborhood before estimating arithmetic
   collisions. Do not claim such an operator is already bounded.

Hostile audit remains deferred to paper stage.
