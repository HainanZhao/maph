# Cycle 126 preregistration: Freiman web to recurrence chains

Date frozen: 2026-08-02 UTC.

For the Cycle-125 high-multiplicity occupied set `A` and its rational-label
Freiman map `a -> r_a`, fix each represented nonzero difference `d` and prove
that `r_(a+d)/r_a` is independent of the edge base `a`. Decompose the
`d`-edge graph into paths and derive the exact longest-chain lower bound from
its edge count.

Use difference pigeonholing and the Cycle-92 approximation to quantify a
popular multiplier `rho_d`, its error from `g^d`, and accumulated error on a
chain. Freeze the edge-density condition required for a prescribed chain
depth. Success is an exact recurrence compiler and an explicit remaining
anchor/depth gate, not automatic E16 seed realization.
