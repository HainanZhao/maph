# Cycle 152 scope correction: bounded-multiplier divisor-fan inverse

## Correction

The original Cycle 152 theorem and every frozen proof input remain valid.  Its
machine status, however, could be read as advancing the actual E14D-L
negative-tail obstruction.  The theorem is conditional: it assumes that the
licensed smooth strict halo has normalized weights, a uniform complete
per-mode bound, and target negative mass.  It does not prove that the actual
complement supplies those hypotheses.

This correction replaces only the status interpretation with

```text
SEALED_CONDITIONAL_BOUNDED_MULTIPLIER_DIVISOR_FAN_INVERSE.
```

## Unchanged theorem

`PROVED`: under `sum_b w_b<=1`, `0<=n_b<=Cw_b/m_b`, and
`sum_b n_b>=kappa` for fixed positive `C,kappa`, one
`m_0<=ceil(2C/kappa)` carries at least
`kappa/(2ceil(2C/kappa))` negative mass, with

```text
h_b=m_0d_b,       d_b|h,       gcd(m_0,h/d_b)=1.
```

No proof content, convention, test, hash, or replay result in the original
record is changed.

## Correct next target

The project gate is `ACTUAL_STRICT_HALO_MASS_TO_BOUNDED_FAN_OR_ESCAPE_OPEN`.
Cycle 153 must route the actual Cycle-149--151 negative correlation: prove or
reject the normalized-weight, uniform-complete-bound, aggregate-error, and
strict-halo-mass hypotheses.  It must output either an actual labelled
bounded-multiplier fan or a quantitative obligation on boundary,
phase-changing, nonsmooth, or unbounded-tail escape mass.

## Session-companion decision record

The same live session companion `/root/guth_maynard_session_mentor` was
reactivated after Cycle 152 replay.  It identified the scope-label risk and
recommended this correction plus the actual-mass routing target.  The primary
worker adopts that recommendation.  This is containment, not a hostile audit.
