# Cycle 175: full affine eligibility grid or labelled discrepancy bank

## Claim boundary

`PROVED`: a complete Cycle-166 affine parameter fibre has an exact pullback
through Cycle-167 range and divisibility eligibility. All eligible rows retain
one common Cycle-174 capacity ratio and dyadic class. If that breadth is low,
the full parent fibre is retained with an exact range, insoluble-residue, or
signed modular-discrepancy certificate.

This proves no breadth lower bound for an actual massed fibre, no target-local
packet, recurrence, density, or interval gain.

## Exact full-fibre ledger

For a finite retained parameter set `N`, write `h(n)=h0+r n`. Intersect both
frozen row ranges first:

```text
M={n in N: H<=h(n)<=2H and H<=q h(n)/a<=2H}.         (1)
```

Put `g0=gcd(a,r)`. If `g0` does not divide `h0`, the divisibility condition
`a|h0+r n` has no solution and the complete fibre is an insoluble-residue
bank. Otherwise it is one fixed class

```text
n=n0 (mod m),       m=a/g0,
N_elig=M intersect (n0+m Z).                         (2)
```

Thus every selected row has the exact beta-preserving C167 map. Since the
cross-edge state `(a,q,K,H)` is fixed before `n` is selected, all selected
rows share exactly one capacity ratio `qK/H`, and hence exactly one Cycle-174
saturated/deficit class.

## Breadth and the retained alternative

Define `b=|N_elig|` and the signed residue discrepancy

```text
Disc=b-|M|/m.                                        (3)
```

The record is a transport grid of `b` distinct labelled cross edges, not a
claim that their parent multiplicity transfers. If `b` is below a later
frozen breadth threshold, retain instead: the insoluble residue condition;
the labelled range intersection `M`; or `(n0,m,Disc)` together with every
parent and row label. These alternatives are exhaustive.

## Necessity of the discrepancy branch

Take `h0=25,r=1,a=5,q=4,H=20,K=5`. The whole parameter set

```text
N={1,2,3,4,6,7,8,9}
```

lies in both row ranges and has the unique eligible class `n=0 (mod 5)`, yet
has `b=0` and `Disc=-8/5`. It may still have many primitive four-subsets.
Therefore parent multiplicity alone cannot force eligible breadth; dropping
this labelled discrepancy would be invalid.

## Consequence

The next bridge must use actual exponential/fibre information to lower-bound
this eligible breadth or to turn a massed discrepancy/range/capacity bank into
a quantitative inverse theorem. The algebraic fibre interface alone is now
exhausted.
