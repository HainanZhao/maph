# Cycle 125 preregistration: high-multiplicity Freiman ray web

Date frozen: 2026-08-02 UTC.

Use Cycle 92's dyadic collision web. For each occupied mode `a`, freeze its
injective reduced rational label `r_a=p_a/q_a`, its multiplicity in `[M,2M)`,
and the bound `q_a<<Q/M`. For additive quadruples
`a1+a2=a3+a4`, compare `r_a1 r_a2` with `r_a3 r_a4` using the common
exponential target.

Prove exact equality in the high-multiplicity range `M^4K>>Q^3`; record the
complementary multiplicity threshold and the exact additive-energy lower
bound. Translate exact product identities into prime-valuation identities.
Success is a labelled Freiman web plus an explicit codegree/anchor gate for
E16, not an automatic transport seed or analytic collision bound.
