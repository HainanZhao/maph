# Cycle 46 inverse-wrap curvature preregistration v1

## Claim boundary

This cycle may translate `1/X`-scale clustering of the curved wrap
frequencies into near-integer points on an inverse logarithmic curve and
compute its exact critical exponents. It may not promote the required
lattice-point bound, de-aliasing, `LCAM_s`, density, or interval gain.

## Registered tasks

1. Invert `h(exp(2pi k/Delta)-1)=j+beta+O(1/X)` with uniform constants.
2. Compute the tube width and first two derivative scales of the inverse
   curve.
3. At `h=X^(11/25)`, compare the `h^(7/11)` de-aliasing target with the
   reciprocal curvature scale.
4. State the exact lattice-point theorem sufficient for Cycle 45.

Hostile audit remains deferred to paper stage.
