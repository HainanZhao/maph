# Cycle 115 preregistration: local weak-turnover elimination

Date frozen: 2026-08-02 UTC.

Replace Cycle 97's global interval `[x-1,x+1]` by
`I=[x/2,3x/2]`. Freeze `Lx=S2 exp(3Mx/2)` and
`ellx=S2 exp(-3Mx/2)`. Prove an exhaustive local alternative: a Newton root,
a nearby critical point, or the explicit lower bound
`delta>ellx^2 x^2/(16Lx)`. Then specialize to `x=2pi/D`, `M<=D`. No branch
closure may be claimed until the registered tolerance is compared to this
threshold.
