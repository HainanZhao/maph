# Cycle 124 preregistration: bilinear self-duality contract

Date frozen: 2026-08-02 UTC.

In the Cycle-123 leading operator set `(a,b)=(u,u+v)` and
`alpha=q0/p0`. On fixed compact charts, separate the coupled smooth factor in
the normalized variables `(ell/K,n'g^a/Q,alpha m g^b/Q)` at `X^epsilon`
rank and power-negligible error. Preserve all phase and anchor labels.

Reduce each separated term to a product of sparse-exponential sums and derive
the exact Cauchy--Schwarz second-moment target. Compare that target with the
Cycle-87 diagonal target. Success is either a strict exponent gain or a
scoped norm-level self-duality lemma with an explicit excess-energy inverse.
Do not describe norm-level self-duality as an impossibility theorem for all
methods.
