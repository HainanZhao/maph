# Cycle 134 preregistration: transition entropy and tail anchor

Date frozen: 2026-08-02 UTC.

For one primitive rational column `(p,q)` and orientation `s`, classify every
integer column `(P,R)` satisfying `Pq-pR=s`.  Count the lifts with `R~S`
when `q~N`, and propagate this shear freedom to the transition matrices
`U_b U_a^{-1}`.

Then restore the exact phase datum discarded by the determinant lift.  Use
the consecutive-convergent shell to define a normalized residual coordinate
and recover the exact collision error from `(p,q,R)` plus that coordinate.

Success is a scoped data-loss theorem for compilers retaining only rational
labels, orientations, determinants, and dyadic sizes, together with the
minimal additional tail/phase coordinate needed by the next engine.  It is
not a no-go theorem for the actual phase-coupled operator.
