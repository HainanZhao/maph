# Cycle 164: common-wrap valuation web or weighted Sidon

`PROVED`: conditional on the Cycle-163 common-wrap arm, fibers with
`D_m^2/E_m>=H/2` retain at least half its squared edge mass. A frozen dyadic
weight refinement retains comparable-weight leaf sets of effective cardinality
`X^(1/600-o(1))` up to logarithmic loss. Every additive quadruple in one such
fixed-wrap set gives the exact product relation `q1q2=q3q4` because the
integer residual is `O(Q/K)<1`.

At energy `E_+(A)>=|A|^2X^(1/1200)`, this yields an exact valuation web;
below it the retained family is a `PROVED` weighted-Sidon obstruction. Neither
branch is a transport seed, moment bound, density result, or interval result.

Indeed low fibers contribute `<(H/2)sum E_m<=G/2`. In a fixed wrap,
`q_de^(2pi d/D)=Y+O(K^-1)`. If `d1+d2=d3+d4`, multiplication and cancellation
of exponential factors gives `|q1q2-q3q4|<<Q/K=X^(-11/25+o(1))<1`; integrality
forces equality, hence the prime-by-prime valuation identity.
