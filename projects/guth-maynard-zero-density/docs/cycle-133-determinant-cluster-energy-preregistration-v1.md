# Cycle 133 preregistration: determinant-cluster energy compiler

Date frozen: 2026-08-02 UTC.

Let an excessive Cycle-132 block have mode set `A`, denominators `q~N`, next
denominators `R~S`, rational labels `x_a=p_a/q_a`, and consecutive matrices
`U_a=[[P_a,p_a],[R_a,q_a]]` of determinant `s_a in {+1,-1}`.

For every additive quadruple in `A`, compare the `O(1/(NS))` rational-label
product error with the exact lower bound for two distinct rationals of
denominator `O(N^2)`.  Determine the exact `(rho,tau)` integer-forcing
region and the subrange forced by `S>>KQ/N`.

Retain both the prime-valuation Freiman map and the integral transition
matrices `T_{a,b}=U_b U_a^{-1}`.  Check the cocycle law exactly.  Success is
a strict extension of the exact multiplicative-web range and a precise
statement of the additional transition invariant needed to obtain chain
depth or a phase seed.  Exact multiplicative energy alone must not be called
a recurrence theorem.
