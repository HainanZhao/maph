# Cycle 164 preregistration: common-wrap valuation web or weighted Sidon

Date frozen: 2026-08-02 UTC.

In the globally massed common-wrap arm of Cycle 163 freeze
`G=sum_mD_m^2`, `E=sum_mE_m`, and `H=X^(1/600-o(1))`. Retain fibers with
`D_m^2/E_m>=H/2`; they must carry at least `G/2`. Use a frozen dyadic-weight
refinement, losing only logarithmic mass, to obtain comparable-weight leaf
sets `A` of effective size `>=X^(1/600-o(1))`.

Freeze the compact chart, `K/Q`, and the threshold at which `Q/K<1`. In one
fixed-wrap fiber,

```text
q_d exp(2pi d/D)=Y+O(K^(-1)).
```

For every `d1+d2=d3+d4`, prove the exact integer consequence
`q1q2=q3q4` from `|q1q2-q3q4|<<Q/K<1`, and retain all coefficient, wrap,
orientation, atom, and phase labels.

Freeze `E_+(A)=|A|^2X^(1/1200)`. If the additive energy meets this threshold,
bank an exact valuation web with the full prime-by-prime product relation. If
not, preserve the globally massed comparable-weight family as a weighted-Sidon
obstruction. A retained quadruple violating the exact product, or a loss
beyond the frozen logarithmic budget, falsifies the target.

This is E16 input, not a transport seed; it proves no moment, density, or
interval result.

## Companion checkpoint

The persistent companion `/root/guth_maynard_session_mentor` was live-checked
and reactivated under its stable identity on 2026-08-02 UTC. It selected this
web-or-Sidon target and required the weighted-Sidon alternative to remain
explicit. The primary adopts the recommendation.
