# Cycle 128: frozen aliases are sparse, primitive, and convergent

`OBSERVED`. The frozen 80-decimal profiler produced 18 rows from three
`D`-scales, three lower-band frequency exponents, and residual radii `1/K`
and `4/K`.

At radius `1/K`, the nine grids contain 16 hits on 16 occupied modes. At
radius `4/K`, they contain 48 hits on 48 occupied modes. Every observed ray
has multiplicity one; none crosses the finite-grid Cycle-125
high-multiplicity threshold. Every reduced rational label is a continued-
fraction convergent of its target `exp(2pi a/D)` before denominator `2Q`.

All observed counts remain below the target `Q`. The largest hit/target ratio
is `9/13` on `(D,xi,radius)=(108,16/25,4)`. Counts also lie below the crude
volume proxy in every row; the largest hit/volume ratio is about `0.596`.
The most structured occupied set occurs on the radius-four `D=108`,
`xi=16/25` grid, where the most popular difference supports a chain of only
three edges.

These observations favor a theorem that treats multiplicity-one convergents
as the minor-arc bulk and reserves the Cycle-125/126 Freiman compiler for a
rare high-multiplicity branch. They do not prove asymptotic sparsity,
continued-fraction exhaustiveness, a sampled-Mellin estimate, or a collision
bound.
