# Cycle 109 discovery candidate: bound the full kernel, not its remainder

`CONJECTURED` before proof sealing.

On an actual scale ray the three logarithmic phases acquire one common large
parameter `ell`, while their stationary points stay on fixed compact charts.
Their second derivatives have magnitude comparable to `ell` and fixed signs.
An iterated one-dimensional oscillatory-integral estimate should therefore
bound the complete joint kernel by `C_W ell^(-3/2)`, without splitting it into
leading and remainder terms. Absolute summation would close the scale orbit
even at exact phase resonance.
