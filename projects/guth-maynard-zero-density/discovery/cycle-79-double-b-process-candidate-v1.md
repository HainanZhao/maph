# Cycle 79 discovery note: double B-process candidate

Status: `EXPLORATORY`. Nothing in this note is proof.

For the Cycle-77 saddle, Fourier expansion of the vertical tube suggests

```text
S_k=sum_(d~Delta) sum_(q~Q) e(k c_0 q exp(beta d/Delta)),
k<=eta^-1=X^(83/75).
```

The candidate two-dimensional stationary equations after Poisson summation
in `(d,q)` are

```text
r=k c_0 exp(beta d/Delta),
h=beta q r/Delta.
```

They give `r~k`, `h~kQ/Delta`, inverse map

```text
d=(Delta/beta)log(r/(kc_0)),
q=hDelta/(beta r),
```

and candidate dual phase

```text
Psi=(hDelta/beta)log(kc_0/r).
```

At the maximum `k`, the dual `h` exponent appears to be `21/25`, matching
the earlier skeleton cardinality. Frequencies below `Delta/Q=X^(4/15)` have
no positive `h` stationary range and must be separated before promotion.

These identities, the exact Fourier target, stationary determinant,
amplitude, support, and boundary split are to be independently derived under
the Cycle-79 preregistration.
