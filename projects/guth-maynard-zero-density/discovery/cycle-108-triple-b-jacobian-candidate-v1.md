# Cycle 108 discovery candidate: the scale Jacobian is summable

`CONJECTURED` before proof sealing.

The three one-dimensional B-process amplitudes on the actual scale ray are

```text
sqrt(c*Delta)/m,
sqrt(c*H)/n,
sqrt(c*(H-Delta))/n'.
```

Simultaneous scaling by `ell` should multiply each by `ell^(-1/2)`, hence
their product by `ell^(-3/2)`. The stationary evaluation points are ratios of
two scaled variables and should remain fixed. Therefore the leading
perfect-power scale family is absolutely summable even at exact phase
resonance, provided the residual arithmetic weight has a subpower envelope.
