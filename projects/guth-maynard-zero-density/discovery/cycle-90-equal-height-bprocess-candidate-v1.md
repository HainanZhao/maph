# Cycle 90 discovery candidate: equal height is a shorter saddle problem

`CONJECTURED`: the `h=h'` branch should not be attacked as a length-`K`
Dirichlet polynomial.  Poisson/stationary phase in `r` sends it to a
length-`Q` polynomial, while the `h` sample count is `H=KQ/D>Q` throughout
the lower signed band.

The remaining dual collision is expected to be

```text
|n'-n exp(2pi a/D)| << 1/K,
a~D, n,n'~Q.
```

Its volume is `DQ/K`, below the target `Q` by the power `K/D`.  Its surface
has nonzero affine Hessian determinant.  The candidate theorem is a signed
or incidence bound of size `Q X^o(1)` for this saddle collision, uniform in
the smooth weights inherited from Cycle 81.

This note is discovery only and proves no transformation formula or bound.

