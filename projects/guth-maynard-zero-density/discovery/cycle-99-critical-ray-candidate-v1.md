# Cycle 99 discovery candidate: near-double roots are rational rays

`CONJECTURED`: in the opposite-sign mode branch, put

```text
w=a-b,
r=-Cb/(Ba)=C|b|/(B|a|)>0.
```

The unique critical point satisfies `r=exp(w t*)`. If Cycle 97 places
`t*` near `x=2pi/D`, then `r` lies near `exp(2pi w/D)`. Since the reduced
height of `r` is at most `QM`, Farey spacing should make the rational label
unique once the critical error is below `(QM)^(-2)`, while exponential
spacing should make labels injective across distinct `w`.

Any remaining multiplicity would then be a concrete factorization fiber
`C|b|/(B|a|)=r`, not an anonymous near-stationary loss.
