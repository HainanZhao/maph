# Cycle 101 discovery candidate: compact rational packing sums generic fibers

`CONJECTURED`: Cycle 99 makes strong critical labels injective across signed
mode differences `w`, and the compact stationary support puts every label
`N/R` in `[exp(-L),exp(L)]`. If `z=min(N,R)`, the number of reduced labels
with `z<=Y` is `O_L(Y^2)`. Therefore for `J` distinct labels,

```text
sum 1/z <<_L sqrt(J).
```

Cycle 100 gives `F_generic<=2Q tau(|w|)/z`; with `J<=4M`, the aggregate
generic strong-critical fiber should be

```text
sum_w F_generic(w) <<_L Q M^(1/2+o(1)).
```

This does not address cross-valuations or weak localization.
