# Cycle 107 discovery candidate: the scale saturator is a geometric phase

`CONJECTURED` before proof sealing.

Restoring `(A,B,C)=(p0*n,p0*n',q0*m)` restricts the Cycle-106 coefficient
scale to a sublattice `lambda=lambda0*ell`. The indices `(n,n',m)` then scale
simultaneously by `ell`.

The Cycle-94 entropy phase depends on stationary heights and indices through
scale-invariant ratios, with one linear prefactor. Scaling both heights and
all three indices by `ell` should therefore give

```text
Phi_ell=ell*Phi_0.
```

The exact all-scale unsigned saturator would then be a weighted geometric
sum. Cancellation fails only when the base phase is near an integer, which
is a payload-bearing inverse output rather than another divisor web.
