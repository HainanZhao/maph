# Cycle 100 discovery candidate: exact critical-fiber divisor switch

`CONJECTURED`: fix a signed critical label `(w,N,R)`, with `(N,R)=1`.
Opposite signs force

```text
s=|a|, t=|b|, s+t=|w|=:W.
```

For each split, the fiber equation is `CtR=BsN`. If
`g=(sN,tR)`, all positive solutions should be

```text
B=lambda*tR/g,   C=lambda*sN/g.
```

Writing `g0=(s,t)` appears to factor the gcd exactly as

```text
g=g0*(s/g0,R)*(t/g0,N).
```

Thus the generic fiber has only the ordinary split gcd, while every excess
is a cross-valuation web between the mode split and the rational label.

No Möbius sign is assumed: the Cycle-87 stationary branch retains oscillatory
phases and B-process amplitudes, while Cycle 66's Möbius sign belongs to a
different representation unless a bridge is proved.
