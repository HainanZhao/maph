# Cycle 93 discovery candidate: the sub-alias branch is negligible

`CONJECTURED`: for integer `0<|Delta h|<<K/D`, put
`t=D Delta h/(2pi)`.  The `k` cross phase is `t log k`.  After Poisson in
`k` and rescaling `k=Kx`, the `m=0` derivative has size `|t|>=cD`, while
every `m!=0` derivative has size comparable to `K` because `|t|/K` stays in
a fixed interval strictly below one. Fixed smooth cutoffs should therefore
give arbitrary power decay before all polynomial support sums.

This is discovery only and proves no branch estimate.

