# Cycle 170 working decision summary v1

`PROVED` and sealed at
`artifacts/cycle-170-projective-packet-lift-v1.json`; the frozen question is
in `docs/cycle-170-projective-packet-lift-preregistration-v1.md`.

## Strategic checkpoint

`OBSERVED` mentor decision: use the exponential identity to lift a
source-local packet through a cross edge. The central target data are
`(D,N,g,Q,A,Lambda,K_T)`, not just the projective gcd.

High content alone may fail because the source-packet error is multiplied by
`a`, edge error is multiplied by `d+b`, or `|Q|K_T>H`. Integrality and range
of the transported beta seed remain independent labels.

## First tasks

- `CONJECTURED`: verify the signed lift identity and exact reduced error.
- `CONJECTURED`: make depth and packet admissibility separate gates.
- `CONJECTURED`: build finite examples for a successful seeded lift and for
  content/error/admissibility failures before any population claim.

## First exact lift

`PROVED` finite identity: with `alpha_ell=1/2`, `E=3/2`, source packet
`d=2,b=1`, and edge `q=2,a=3`, the target relation is
`4 alpha_L-5=0`; the eligible beta seed `(h,j)=(30,15)` transports exactly to
`(h^+,j^+)=(20,25)`. The signed convention also separates an error-supported
depth from denominator capacity and tests their obstruction order.

This verifies the algebraic engine, not a population of eligible source pairs
or a deep target packet in the actual census. The session companion approved
the seal: the zero-load convention, beta-seed identity, reduced packet
inequality, and `QK_T<=H` replay in one exact classifier. The next decision
must force compatible projective-lift population or quantify an obstruction
bank; a further finite example is non-progress.
