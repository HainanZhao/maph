# Cycle 105 discovery candidate: rational aliases are powered rays

`CONJECTURED` before proof sealing.

Cycle 104 gives `N=n0^d`, `R=r0^d`, while the primitive mode degree is
`w=h*d`. Therefore the strong critical datum is exactly

```text
(w,N/R)=(h*d,(n0/r0)^d).
```

Taking a `d`th root transfers the Cycle-99 phase error to the base ray near
`exp(h*x)`. Repetition of one `(h,n0/r0)` gives exact arithmetic mode
multiples and geometric rational labels. Missing exponents must remain
missing; the output is a powered-ray seed, not automatically a complete
progression.
