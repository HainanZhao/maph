# Cycle 172 working decision summary v1

`PROVED` and sealed at
`artifacts/cycle-172-primitive-eligible-fibre-moment-v1.json`; frozen
question: `docs/cycle-172-primitive-eligible-fibre-moment-preregistration-v1.md`.

## Engine

`CONJECTURED`: the actual Cycle-165 source packet is reduced, so the Cycle 171
source-core factor collapses to one and projective content is the coprime
two-factor product `u v`. The first task is not to assume that this creates a
moment surplus: build an exact labelled affine-fibre model satisfying the
existing local interface with `u=v=1` and classify its first target failure.

## Advance rule

If the model survives, it is a scoped **signed abstract** structural no-go:
local primitive eligibility, range, balance, and seed data do not force
`M>W`. Its negative alpha values lie outside the actual positive exponential
curve, so it does not constrain the real Census or refute a global geometric
coupling theorem. If it fails, retain the first failed condition and pursue
the corresponding positive invariant. The companion approved seal only after
the boundary explicitly confined the negative-alpha family to the signed
abstract interface. The actual positive-exponential primitive-fibre question
is the next authorized gate.
