# Cycle 94 discovery candidate: triple-B entropy and anchor differences

`CONJECTURED`: apply the smooth B-process separately to the stationary
`k` cross term and the two logarithmic `r,r'` columns. With `h'=h-Delta`,
the three stationary values cancel their linear terms and leave

```text
Delta log(c0 Delta/m)-h log(h/n)+h' log(h'/n').
```

The central stationary equations in `(h,Delta)` appear to force
`h'/h=n'/n` and `m=c0(n-n')`. The phase is homogeneous of degree one in
`(h,Delta)`, so its Hessian should be degenerate and noncentral Poisson modes
must be retained as projective aliases.

This note is discovery only.

