# Cycle 188 working ledger

For `V=T^a`, full fibres at labels `n,2n` have pair masses at most
`T^(44-2a)` and `T^(44-4a)`. Their cross mass is `T^(88-6a)`, at most
`T^28` in the light regime `a>=10`, below `T^42`. Labels `j>=3` have no
non-singleton fibre when `a>=10`. This is an actual corrected phase family;
the pending checks are its exact residual and chart bookkeeping.
